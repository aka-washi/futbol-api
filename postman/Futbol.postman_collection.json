{
  "info": {
    "_postman_id": "3e4f9c53-79f0-4889-8cb2-8f58b2982919",
    "name": "Futbol",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_exporter_id": "1879809"
  },
  "item": [
    {
      "name": "bootstrap",
      "item": [
        {
          "name": "POST - /bootstrap/load",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/bootstrap/load",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "bootstrap",
                "load"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"entityType\": \"team\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "POST - /bootstrap/health",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/bootstrap/health",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "bootstrap",
                "health"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "POST - /bootstrap/load-all",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/bootstrap/load-all",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "bootstrap",
                "load-all"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "competition",
      "item": [
        {
          "name": "Get all - /competitions",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /competitions",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tournamentSeasonDisplayName\": \"Liga MX 2024-2025\",\n  \"displayName\": \"Liga MX Apertura 2024\",\n  \"type\": \"Apertura\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2024-12-15\",\n  \"status\": \"Completed\",\n  \"totalMatches\": 153,\n  \"description\": \"First half of the 2024-2025 Liga MX season\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /competitions/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /competitions/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tournamentSeasonDisplayName\": \"Liga MX 2024-2025\",\n  \"displayName\": \"Liga MX Apertura 2024\",\n  \"type\": \"Apertura\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2024-12-15\",\n  \"status\": \"Completed\",\n  \"totalMatches\": 153,\n  \"description\": \"First half of the 2024-2025 Liga MX season\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /competitions/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Completed\",\n  \"description\": \"First half of the 2024-2025 Liga MX season\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /competitions/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /competitions/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/competitions/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "competitions",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "country",
      "item": [
        {
          "name": "Get all - /countries",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /countries",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Mexico\",\n  \"code\": \"MX\",\n  \"isoCode\": \"MEX\",\n  \"displayName\": \"Mexico\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /countries/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /countries/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Mexico\",\n  \"code\": \"MX\",\n  \"isoCode\": \"MEX\",\n  \"displayName\": \"Mexico\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /countries/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Mexico\",\n  \"isoCode\": \"MEX\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /countries/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /countries/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/countries/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "countries",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "group",
      "item": [
        {
          "name": "Get all - /api/groups",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /api/groups",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Group A\",\n  \"displayName\": \"Group A\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"order\": 1,\n  \"numberOfTeams\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /api/groups/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /api/groups/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Group A\",\n  \"displayName\": \"Group A\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"order\": 1,\n  \"numberOfTeams\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /api/groups/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Group A\",\n  \"name\": \"Group A\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /api/groups/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /api/groups/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/groups/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "groups",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "lineup",
      "item": [
        {
          "name": "Get all - /lineups",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /lineups",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Club América - Matchday 1\",\n  \"matchDisplayName\": \"Club América vs Chivas Monterrey\",\n  \"teamDisplayName\": \"Club América\",\n  \"formation\": \"4-3-3\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /lineups/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /lineups/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Club América - Matchday 1\",\n  \"matchDisplayName\": \"Club América vs Chivas Monterrey\",\n  \"teamDisplayName\": \"Club América\",\n  \"formation\": \"4-3-3\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /lineups/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"formation\": \"4-3-3\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /lineups/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /lineups/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/lineups/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "lineups",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "lineupMember",
      "item": [
        {
          "name": "Get all - /api/lineup-members",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /api/lineup-members",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Cuauhtémoc Blanco - Starting\",\n  \"lineupDisplayName\": \"Club América - Matchday 1\",\n  \"personType\": \"Player\",\n  \"playerPersonFirstName\": \"Cuauhtémoc\",\n  \"playerPersonLastName\": \"Blanco\",\n  \"roleType\": \"Starting\",\n  \"position\": \"Forward\",\n  \"jerseyNumber\": 10,\n  \"captain\": true,\n  \"orderNum\": 1\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /api/lineup-members/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /api/lineup-members/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Cuauhtémoc Blanco - Starting\",\n  \"lineupDisplayName\": \"Club América - Matchday 1\",\n  \"personType\": \"Player\",\n  \"playerPersonFirstName\": \"Cuauhtémoc\",\n  \"playerPersonLastName\": \"Blanco\",\n  \"roleType\": \"Starting\",\n  \"position\": \"Forward\",\n  \"jerseyNumber\": 10,\n  \"captain\": true,\n  \"orderNum\": 1\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /api/lineup-members/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"position\": \"Forward\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /api/lineup-members/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /api/lineup-members/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/api/lineup-members/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "api",
                "lineup-members",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "match",
      "item": [
        {
          "name": "Get all - /matches",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /matches",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Club América vs Chivas Monterrey - Matchday 1\",\n  \"displayName\": \"Club América vs Chivas Monterrey\",\n  \"matchdayDisplayName\": \"Matchday 1\",\n  \"venueDisplayName\": \"Estadio Azteca\",\n  \"homeTeamDisplayName\": \"Club América\",\n  \"awayTeamDisplayName\": \"Chivas Monterrey\",\n  \"matchDate\": \"2024-07-05T20:00:00\",\n  \"homeScore\": 2,\n  \"awayScore\": 1,\n  \"status\": \"Completed\",\n  \"attendance\": 87523\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /matches/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /matches/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Club América vs Chivas Monterrey - Matchday 1\",\n  \"displayName\": \"Club América vs Chivas Monterrey\",\n  \"matchdayDisplayName\": \"Matchday 1\",\n  \"venueDisplayName\": \"Estadio Azteca\",\n  \"homeTeamDisplayName\": \"Club América\",\n  \"awayTeamDisplayName\": \"Chivas Monterrey\",\n  \"matchDate\": \"2024-07-05T20:00:00\",\n  \"homeScore\": 2,\n  \"awayScore\": 1,\n  \"status\": \"Completed\",\n  \"attendance\": 87523\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /matches/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Completed\",\n  \"attendance\": 87523\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /matches/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /matches/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matches/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matches",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "matchEvent",
      "item": [
        {
          "name": "Get all - /match-events",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /match-events",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Goal - Cuauhtémoc Blanco 23'\",\n  \"matchDisplayName\": \"Club América vs Chivas Monterrey\",\n  \"teamDisplayName\": \"Club América\",\n  \"playerPersonFirstName\": \"Cuauhtémoc\",\n  \"playerPersonLastName\": \"Blanco\",\n  \"type\": \"Goal\",\n  \"period\": \"First Half\",\n  \"minute\": 23,\n  \"extraMinute\": 0,\n  \"description\": \"Header from center of the box to bottom right corner. Assisted by cross from the right wing.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /match-events/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /match-events/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Goal - Cuauhtémoc Blanco 23'\",\n  \"matchDisplayName\": \"Club América vs Chivas Monterrey\",\n  \"teamDisplayName\": \"Club América\",\n  \"playerPersonFirstName\": \"Cuauhtémoc\",\n  \"playerPersonLastName\": \"Blanco\",\n  \"type\": \"Goal\",\n  \"period\": \"First Half\",\n  \"minute\": 23,\n  \"extraMinute\": 0,\n  \"description\": \"Header from center of the box to bottom right corner. Assisted by cross from the right wing.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /match-events/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"minute\": 23,\n  \"description\": \"Header from center of the box to bottom right corner. Assisted by cross from the right wing.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /match-events/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /match-events/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/match-events/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "match-events",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "matchday",
      "item": [
        {
          "name": "Get all - /matchdays",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /matchdays",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Matchday 1\",\n  \"displayName\": \"Matchday 1\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"number\": 1,\n  \"startDate\": \"2024-07-05\",\n  \"endDate\": \"2024-07-07\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /matchdays/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /matchdays/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Matchday 1\",\n  \"displayName\": \"Matchday 1\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"number\": 1,\n  \"startDate\": \"2024-07-05\",\n  \"endDate\": \"2024-07-07\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /matchdays/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Completed\",\n  \"displayName\": \"Matchday 1\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /matchdays/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /matchdays/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "GET - /matchdays/name/{name}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/name/{{name}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "name",
                "{{name}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "GET - /matchdays/displayName/{displayName}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/matchdays/displayName/{{displayName}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "matchdays",
                "displayName",
                "{{displayName}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "organization",
      "item": [
        {
          "name": "Get all - /organizations",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /organizations",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Federación Mexicana de Fútbol\",\n  \"displayName\": \"FMF\",\n  \"type\": \"Federation\",\n  \"abbreviation\": \"FMF\",\n  \"countryDisplayName\": \"Mexico\",\n  \"foundedDate\": \"1927-08-23\",\n  \"description\": \"Mexican Football Federation\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /organizations/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /organizations/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Federación Mexicana de Fútbol\",\n  \"displayName\": \"FMF\",\n  \"type\": \"Federation\",\n  \"abbreviation\": \"FMF\",\n  \"countryDisplayName\": \"Mexico\",\n  \"foundedDate\": \"1927-08-23\",\n  \"description\": \"Mexican Football Federation\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /organizations/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"website\": \"https://ligamx.net/\",\n  \"description\": \"Mexican Football Federation\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /organizations/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /organizations/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/organizations/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "organizations",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "person",
      "item": [
        {
          "name": "Get all - /persons",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /persons",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /persons/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /persons/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /persons/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lastName\": \"Hernández\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /persons/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /persons/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/persons/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "persons",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "player",
      "item": [
        {
          "name": "Get all - /players",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /players",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"person\": {\n    \"firstName\": \"Cuauhtémoc\",\n    \"lastName\": \"Blanco\",\n    \"birthDate\": \"1973-01-17\",\n    \"gender\": \"Male\",\n    \"birthPlace\": \"Mexico City\",\n    \"birthCountryDisplayName\": \"Mexico\",\n    \"nationalityCountryDisplayName\": \"Mexico\",\n    \"height\": 1.78,\n    \"weight\": 82\n  },\n  \"position\": \"Forward\",\n  \"preferredFoot\": \"Right\",\n  \"active\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /players/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /players/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"person\": {\n    \"firstName\": \"Cuauhtémoc\",\n    \"lastName\": \"Blanco\",\n    \"birthDate\": \"1973-01-17\",\n    \"gender\": \"Male\",\n    \"birthPlace\": \"Mexico City\",\n    \"birthCountryDisplayName\": \"Mexico\",\n    \"nationalityCountryDisplayName\": \"Mexico\",\n    \"height\": 1.78,\n    \"weight\": 82\n  },\n  \"position\": \"Forward\",\n  \"preferredFoot\": \"Right\",\n  \"active\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /players/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"position\": \"Forward\",\n  \"preferredFoot\": \"Right\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /players/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /players/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/players/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "players",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "pointSystem",
      "item": [
        {
          "name": "Get all - /pointSystems",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /pointSystems",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Standard 3-1-0\",\n  \"displayName\": \"Standard 3-1-0\",\n  \"description\": \"Standard point system: 3 points for win, 1 for draw, 0 for loss\",\n  \"pointsForWin\": 3,\n  \"pointsForDraw\": 1,\n  \"pointsForLoss\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /pointSystems/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /pointSystems/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Standard 3-1-0\",\n  \"displayName\": \"Standard 3-1-0\",\n  \"description\": \"Standard point system: 3 points for win, 1 for draw, 0 for loss\",\n  \"pointsForWin\": 3,\n  \"pointsForDraw\": 1,\n  \"pointsForLoss\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /pointSystems/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"description\": \"Standard point system: 3 points for win, 1 for draw, 0 for loss\",\n  \"displayName\": \"Standard 3-1-0\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /pointSystems/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /pointSystems/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/pointSystems/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "pointSystems",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "season",
      "item": [
        {
          "name": "Get all - /seasons",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /seasons",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"2024-2025\",\n  \"displayName\": \"2024-2025\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /seasons/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /seasons/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"2024-2025\",\n  \"displayName\": \"2024-2025\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /seasons/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"2024-2025\",\n  \"name\": \"2024-2025\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /seasons/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /seasons/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/seasons/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "seasons",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "seasonTeam",
      "item": [
        {
          "name": "Get all - /season-teams",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /season-teams",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"seasonDisplayName\": \"2024-2025\",\n  \"teamDisplayName\": \"Club América\",\n  \"joinedDate\": \"2024-07-01\",\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /season-teams/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /season-teams/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"seasonDisplayName\": \"2024-2025\",\n  \"teamDisplayName\": \"Club América\",\n  \"joinedDate\": \"2024-07-01\",\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /season-teams/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"seasonDisplayName\": \"2024-2025\",\n  \"teamDisplayName\": \"Club América\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /season-teams/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /season-teams/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/season-teams/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "season-teams",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "staff",
      "item": [
        {
          "name": "Get all - /staff",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /staff",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"person\": {\n    \"firstName\": \"Carlo\",\n    \"lastName\": \"Ancelotti\",\n    \"birthDate\": \"1959-06-10\",\n    \"gender\": \"Male\",\n    \"birthPlace\": \"Reggiolo\",\n    \"birthCountryDisplayName\": \"Italy\",\n    \"nationalityCountryDisplayName\": \"Italy\"\n  },\n  \"role\": \"Head Coach\",\n  \"active\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /staff/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /staff/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"person\": {\n    \"firstName\": \"Carlo\",\n    \"lastName\": \"Ancelotti\",\n    \"birthDate\": \"1959-06-10\",\n    \"gender\": \"Male\",\n    \"birthPlace\": \"Reggiolo\",\n    \"birthCountryDisplayName\": \"Italy\",\n    \"nationalityCountryDisplayName\": \"Italy\"\n  },\n  \"role\": \"Head Coach\",\n  \"active\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /staff/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"role\": \"Head Coach\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /staff/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /staff/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "staff",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "stage",
      "item": [
        {
          "name": "Get all - /stages",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /stages",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"competitionDisplayName\": \"Liga MX Apertura 2024\",\n  \"stageFormatDisplayName\": \"Regular Season - Double Round Robin\",\n  \"order\": 1,\n  \"status\": \"Completed\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2024-11-15\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /stages/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /stages/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"competitionDisplayName\": \"Liga MX Apertura 2024\",\n  \"stageFormatDisplayName\": \"Regular Season - Double Round Robin\",\n  \"order\": 1,\n  \"status\": \"Completed\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2024-11-15\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /stages/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Completed\",\n  \"displayName\": \"Liga MX Apertura 2024 - Regular Season\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /stages/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /stages/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stages/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stages",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "stageFormat",
      "item": [
        {
          "name": "Get all - /stage-formats",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /stage-formats",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Regular Season - Double Round Robin\",\n  \"type\": \"Regular Season\",\n  \"pointSystemDisplayName\": \"Standard 3-1-0\",\n  \"numberOfTeams\": 18,\n  \"hasHomeAndAway\": true,\n  \"description\": \"Standard double round-robin format where each team plays every other team twice\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /stage-formats/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /stage-formats/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Regular Season - Double Round Robin\",\n  \"type\": \"Regular Season\",\n  \"pointSystemDisplayName\": \"Standard 3-1-0\",\n  \"numberOfTeams\": 18,\n  \"hasHomeAndAway\": true,\n  \"description\": \"Standard double round-robin format where each team plays every other team twice\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /stage-formats/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"description\": \"Standard double round-robin format where each team plays every other team twice\",\n  \"displayName\": \"Regular Season - Double Round Robin\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /stage-formats/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /stage-formats/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/stage-formats/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "stage-formats",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "standing",
      "item": [
        {
          "name": "Get all - /standings",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /standings",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Club América - Regular Season\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"teamDisplayName\": \"Club América\",\n  \"position\": 1,\n  \"played\": 17,\n  \"won\": 11,\n  \"drawn\": 4,\n  \"lost\": 2,\n  \"goalsFor\": 32,\n  \"goalsAgainst\": 15,\n  \"goalDifference\": 17,\n  \"points\": 37\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /standings/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /standings/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Club América - Regular Season\",\n  \"stageDisplayName\": \"Liga MX Apertura 2024 - Regular Season\",\n  \"teamDisplayName\": \"Club América\",\n  \"position\": 1,\n  \"played\": 17,\n  \"won\": 11,\n  \"drawn\": 4,\n  \"lost\": 2,\n  \"goalsFor\": 32,\n  \"goalsAgainst\": 15,\n  \"goalDifference\": 17,\n  \"points\": 37\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /standings/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"points\": 37,\n  \"position\": 1\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /standings/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /standings/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/standings/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "standings",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "team",
      "item": [
        {
          "name": "Get all - /teams",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /teams",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"organizationDisplayName\": \"Liga MX\",\n  \"name\": \"Club América\",\n  \"displayName\": \"Club América\",\n  \"abbreviation\": \"AME\",\n  \"gender\": \"Male\",\n  \"ageCategory\": \"Professional\",\n  \"founded\": \"1916-10-12\",\n  \"countryDisplayName\": \"Mexico\",\n  \"status\": \"Active\",\n  \"primaryColor\": \"#FDD835\",\n  \"secondaryColor\": \"#0D47A1\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /teams/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /teams/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"organizationDisplayName\": \"Liga MX\",\n  \"name\": \"Club América\",\n  \"displayName\": \"Club América\",\n  \"abbreviation\": \"AME\",\n  \"gender\": \"Male\",\n  \"ageCategory\": \"Professional\",\n  \"founded\": \"1916-10-12\",\n  \"countryDisplayName\": \"Mexico\",\n  \"status\": \"Active\",\n  \"primaryColor\": \"#FDD835\",\n  \"secondaryColor\": \"#0D47A1\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /teams/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"website\": \"https://clubamerica.com.mx/\",\n  \"status\": \"Active\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /teams/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /teams/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "teams",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "tournament",
      "item": [
        {
          "name": "Get all - /tournaments",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /tournaments",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"organizationDisplayName\": \"Liga MX\",\n  \"name\": \"Liga MX\",\n  \"displayName\": \"Liga MX\",\n  \"type\": \"Domestic League\",\n  \"ageCategory\": \"Professional\",\n  \"level\": 1,\n  \"active\": true,\n  \"description\": \"Mexican top division professional league\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /tournaments/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /tournaments/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"organizationDisplayName\": \"Liga MX\",\n  \"name\": \"Liga MX\",\n  \"displayName\": \"Liga MX\",\n  \"type\": \"Domestic League\",\n  \"ageCategory\": \"Professional\",\n  \"level\": 1,\n  \"active\": true,\n  \"description\": \"Mexican top division professional league\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /tournaments/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"description\": \"Mexican top division professional league\",\n  \"active\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /tournaments/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /tournaments/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournaments/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournaments",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "tournamentSeason",
      "item": [
        {
          "name": "Get all - /tournament-seasons",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /tournament-seasons",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"seasonDisplayName\": \"2024-2025\",\n  \"tournamentDisplayName\": \"Liga MX\",\n  \"displayName\": \"Liga MX 2024-2025\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2025-06-30\",\n  \"status\": \"In Progress\",\n  \"hasRelegation\": true,\n  \"hasPromotion\": true,\n  \"numberOfTeams\": 18,\n  \"description\": \"Mexican top division season 2024-2025\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /tournament-seasons/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /tournament-seasons/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"seasonDisplayName\": \"2024-2025\",\n  \"tournamentDisplayName\": \"Liga MX\",\n  \"displayName\": \"Liga MX 2024-2025\",\n  \"startDate\": \"2024-07-01\",\n  \"endDate\": \"2025-06-30\",\n  \"status\": \"In Progress\",\n  \"hasRelegation\": true,\n  \"hasPromotion\": true,\n  \"numberOfTeams\": 18,\n  \"description\": \"Mexican top division season 2024-2025\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /tournament-seasons/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"In Progress\",\n  \"numberOfTeams\": 18\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /tournament-seasons/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /tournament-seasons/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/tournament-seasons/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "tournament-seasons",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    },
    {
      "name": "venue",
      "item": [
        {
          "name": "Get all - /venues",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create - /venues",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Estadio Azteca\",\n  \"displayName\": \"Estadio Azteca\",\n  \"city\": \"Mexico City\",\n  \"countryId\": 1,\n  \"capacity\": 87523,\n  \"address\": \"Calzada de Tlalpan 3465, Santa Ursula Coapa, Mexico City\",\n  \"surface\": \"Grass\",\n  \"opened\": \"1966-05-29\",\n  \"active\": true,\n  \"imageUrl\": \"https://example.com/azteca.jpg\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get by id - /venues/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update - /venues/{id}",
          "request": {
            "method": "PUT",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Estadio Azteca\",\n  \"displayName\": \"Estadio Azteca\",\n  \"city\": \"Mexico City\",\n  \"countryId\": 1,\n  \"capacity\": 87523,\n  \"address\": \"Calzada de Tlalpan 3465, Santa Ursula Coapa, Mexico City\",\n  \"surface\": \"Grass\",\n  \"opened\": \"1966-05-29\",\n  \"active\": true,\n  \"imageUrl\": \"https://example.com/azteca.jpg\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Patch - /venues/{id}",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"capacity\": 87523,\n  \"displayName\": \"Estadio Azteca\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Delete - /venues/{id}",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues/{{id}}",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Exists - /venues/{id}/exists",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/venues/{{id}}/exists",
              "host": [
                "{{baseHost}}"
              ],
              "port": "{{basePort}}",
              "path": [
                "api",
                "v1",
                "venues",
                "{{id}}",
                "exists"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const method = pm.request.method;",
                  "const requestName = pm.info.requestName || '';",
                  "const path = pm.request.url.getPath();",
                  "",
                  "let allowedCodes = [200];",
                  "",
                  "if (requestName.startsWith('Create - ')) {",
                  "  allowedCodes = [201];",
                  "}",
                  "",
                  "if (requestName.startsWith('POST - /bootstrap/')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "if (requestName.startsWith('Delete - ')) {",
                  "  allowedCodes = [200, 204];",
                  "}",
                  "",
                  "if (requestName.startsWith('Patch - ') || requestName.startsWith('Update - ') || requestName.startsWith('Get ') || requestName.startsWith('Exists - ') || requestName.startsWith('GET - ')) {",
                  "  allowedCodes = [200];",
                  "}",
                  "",
                  "pm.test('Status code matches endpoint expectation', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf(allowedCodes);",
                  "});",
                  "",
                  "pm.test('Response time is under 5s', function () {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "",
                  "const contentType = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
                  "let body = null;",
                  "if (pm.response.code !== 204 && contentType.includes('application/json')) {",
                  "  pm.test('Response is valid JSON', function () {",
                  "    body = pm.response.json();",
                  "    pm.expect(body).to.be.an('object');",
                  "  });",
                  "}",
                  "",
                  "if (body && typeof body === 'object') {",
                  "  pm.test('Response includes message field', function () {",
                  "    pm.expect(body).to.have.property('message');",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Exists - ') && body) {",
                  "  pm.test('Exists endpoint returns boolean data', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(typeof body.data).to.eql('boolean');",
                  "    } else {",
                  "      pm.expect(typeof body).to.eql('boolean');",
                  "    }",
                  "  });",
                  "}",
                  "",
                  "if (requestName.startsWith('Create - ') && body) {",
                  "  pm.test('Create endpoint returns created payload', function () {",
                  "    if (Object.prototype.hasOwnProperty.call(body, 'data')) {",
                  "      pm.expect(body.data).to.be.an('object');",
                  "    }",
                  "  });",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "baseHost",
      "value": "localhost"
    },
    {
      "key": "basePort",
      "value": "8080"
    },
    {
      "key": "baseUrl",
      "value": "http://localhost:8080"
    },
    {
      "key": "id",
      "value": "1"
    },
    {
      "key": "name",
      "value": "Matchday 1"
    },
    {
      "key": "displayName",
      "value": "Matchday 1"
    }
  ]
}
